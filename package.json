{
  "name": "construction-website",
  "version": "1.0.0",
  "description": "ساختمان‌سازی لوکس - وبسایت کامل شرکت ساختمانی",
  "scripts": {
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
